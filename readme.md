# MTOGO Legacy Application

1. Start docker desktop
2. Run `./start.sh` in your terminal to start needed dependencies.
3. Run `npm run dev`

<img src="./imgs/domain_model.png" alt="Domain Model" width="100%"/>
